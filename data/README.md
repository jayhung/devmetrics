# data/

Runtime data directory. The SQLite database (`devmetrics.db`) is created here when you first run the app or sync. This folder is gitignored so your local metrics and config stay off the repo.
